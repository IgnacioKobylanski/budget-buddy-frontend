import "./Header.css"
import Navbar from "../Navbar/Navbar.tsx"

const Header = () => {
    return(
        <header className="header-main">
            <Navbar />
        </header>
    );
};

export default Header;